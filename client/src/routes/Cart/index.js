import React from 'react';
import PropTypes from 'prop-types';
import styles from './Cart.module.css';

const Cart = (props) => {
  return (
    <main>
      <span>Cart</span>
    </main>
  );
};

Cart.propTypes = {};

export default Cart;
