import React from 'react';
import PropTypes from 'prop-types';
import styles from './Cart.module.css';

const Cart = (props) => {
  return (
    <div>
      <span>Cart</span>
    </div>
  );
};

Cart.propTypes = {};

export default Cart;
